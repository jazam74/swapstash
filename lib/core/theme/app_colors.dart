import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary
  static const Color primary = Color(0xFF2563EB);
  static const Color primaryLight = Color(0xFF60A5FA);
  static const Color primaryDark = Color(0xFF1D4ED8);

  // Background
  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Colors.white;

  // Text
  static const Color textPrimary = Color(0xFF111827);
  static const Color textSecondary = Color(0xFF6B7280);

  // Borders
  static const Color border = Color(0xFFE5E7EB);

  // Status
  static const Color success = Color(0xFF16A34A);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFDC2626);
  static const Color info = Color(0xFF0284C7);

  // Trade states
  static const Color tradePending = Color(0xFFF59E0B);
  static const Color tradeAccepted = Color(0xFF2563EB);
  static const Color tradeShipping = Color(0xFFEA580C);
  static const Color tradeCompleted = Color(0xFF16A34A);

  // Chips
  static const Color chipBackground = Color(0xFFF3F4F6);

  // Disabled
  static const Color disabled = Color(0xFF9CA3AF);
}