{
  "name": "swapstash-firestore-tests",
  "description": "Firestore security rules tests for SwapStash (P22A1).",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "cd .. && firestore-tests/node_modules/.bin/firebase emulators:exec --project swapstash-rules-test --only firestore \"node --test --test-concurrency=1 --test-reporter=spec firestore-tests/*.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^14.0.0"
  }
}
